import { Component, OnInit } from '@angular/core';
import { GenericService } from '../lib/services/generic.service';

@Component({
  selector: 'JadeIntegrationUtilsComponent',
  template: ``,
  styles: [
  ],
})
export class JadeIntegrationUtilsComponent implements OnInit {
    constructor(
    ) {
    }

    ngOnInit(): void {
    }

}
